{
  "name": "osi-aggregator-tinyllm",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "api",
      "command": "source .venv/bin/activate && OLLAMA_BASE_URL=http://127.0.0.1:11434 PROMPTS_DIR=/workspace/prompts DATA_DIR=/workspace/data python -m osi_sandbox.api",
      "description": "FastAPI service on :8787. Endpoints: /health, /status, /feeds, POST /run, /reports/latest."
    },
    {
      "name": "worker",
      "command": "source .venv/bin/activate && OLLAMA_BASE_URL=http://127.0.0.1:11434 PROMPTS_DIR=/workspace/prompts DATA_DIR=/workspace/data python -m osi_sandbox.worker",
      "description": "Scheduled passive-feed ingest + broad/fine tiny-LLM analysis loop (POLL_INTERVAL_SEC, default 120s)."
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 8787
    }
  ]
}
